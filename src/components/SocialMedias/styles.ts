import styled from 'styled-components';

export const Button = styled.button`
    background: none;
    border: none;
    cursor: pointer;
    transition: 0.2s;
    &:hover {
        transform: scale(1.1);
    }
`;